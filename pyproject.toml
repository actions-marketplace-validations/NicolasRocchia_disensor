[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "disensor"
version = "0.11.0"
description = "Adversarial, cross-model AI code review with a residue declaration: a CLI and a CI gate that validate the record the review leaves behind."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Nicolas Rocchia", email = "nicolasrocchia@gmail.com" }]
dependencies = ["jsonschema>=4.18"]
keywords = ["code review", "adversarial review", "adversarial code review", "cross-model code review", "multi-model code review", "maker-checker", "ci", "github action", "llm", "ai", "residue declaration", "software quality"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Quality Assurance",
    "Topic :: Software Development :: Testing",
    "Programming Language :: Python :: 3 :: Only",
]

[project.urls]
Homepage = "https://disensor.dev"
Repository = "https://github.com/NicolasRocchia/disensor"
Issues = "https://github.com/NicolasRocchia/disensor/issues"
Changelog = "https://github.com/NicolasRocchia/disensor/blob/main/CHANGELOG.md"
Paper = "https://doi.org/10.5281/zenodo.21633495"

[project.optional-dependencies]
test = ["pytest>=8"]

[project.scripts]
disensor = "disensor.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
disensor = ["residue.schema*.json", "GUIDE.md", "GUIDE.es.md", "prompts/*.md", "report.html"]
